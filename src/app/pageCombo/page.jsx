'use client';
import Header from '../../components/Header';
import PageCombo from '../../components/PageCombo/PageCombo';

export default function pageCombo(){
    return(
        <main>
            <Header/>
            <PageCombo/>
        </main>
    )
}